@page
@model TasteRestaurantApplication.Pages.Admin.FoodType.UpsertModel
@{
    if (Model.FoodType.Id > 0)
    {
        ViewData["Title"] = "Edit FoodType";
    }
    else
    {
        ViewData["Title"] = "Create FoodType";
    }
}


<form method="post">
    <div class="container">

        @if (Model.FoodType.Id != 0)
        {
            <input type="hidden" asp-for="@Model.FoodType.Id" />
        }
        <div class="row px-2 mx-2 backgroundWhite border shadow">
            <div class="text-danger" asp-validation-summary="ModelOnly"></div>
            <div class="col-12 px-3" style="border-bottom:1px solid #325d88">
                <h2 class="text-success">@(Model.FoodType.Id != 0 ? "Edit FoodType": "Create FoodType")</h2>
            </div>

            <div class="col-8 pt-3">
                <div class="form-group row">
                    <div class="col-3">
                        <label asp-for="FoodType.Name"></label>
                    </div>
                    <div class="col-9">
                        <input asp-for="FoodType.Name" class="form-control" placeholder="Name..." />
                        <span asp-validation-for="FoodType.Name" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group row">
                    <div class="col-9 offset-3">
                        <div class="row">
                            <div class="col-6">
                                <button type="submit" class="btn btn-primary form-control">
                                    @(Model.FoodType.Id != 0 ? "Update" : "Create")
                                </button>
                            </div>
                            <div class="col-6">
                                <a asp-page="./Index" class="btn btn-success form-control">Back To List</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>


    </div>
</form>

@section Scripts{

    <partial name="_ValidationScriptsPartial" />
}